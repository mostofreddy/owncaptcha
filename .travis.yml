language: php

php:
    # using major version aliases
    - 5.3
    - 5.4
    - 5.5
    - hhvm

# install phpcodesniffer
# run composer
before_script:
    - pyrus install pear/PHP_CodeSniffer
    - phpenv rehash
    - phpcs --config-set default_standard PEAR
    - composer install

# run phpcodesniffer
script:
    - phpcs --standard=tests/travisci-phpcs.xml --extensions=php .
